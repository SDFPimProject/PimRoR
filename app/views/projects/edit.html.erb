<h1>Editing Project</h1>

<%= form_for(@project, url: project_path, html: { method: :put }) do |f| %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, autofocus: true %>
  </div>
<% end %>

<div>
    <%= Project.human_attribute_name("users") %><br />
    <% @project_users.each do |user| %>
        <p><%= user.email %>
        <%= link_to 'Remove',  project_remove_user_path(@project, user),  method: :post %></p>
    <% end %>
    <%= will_paginate @project_users, :param_name => 'projectUserPage' %>
</div>

<div>
    <%= Project.human_attribute_name("users") %> hinzufügen<br />
    <% @users.each do |user| %>
        <p><%= user.email %>
        <%= link_to 'add',  project_add_user_path(@project, user),  method: :post %></p>
    <% end %>
    <%= will_paginate @users, :param_name => 'userPage' %>
</div>

<%= link_to 'Show', @project %> |
<%= link_to 'Back', projects_path %>
